describe('getTweets', () => {
  // berhasil
  it('should return a list of Tweets', () => {
    expect('test').toEqual('test');
  });

  // if not found
  it('should return a list of Tweets', () => {
    expect('test').toEqual('test');
  });
});
